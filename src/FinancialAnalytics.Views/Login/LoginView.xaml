<UserControl x:Class="FinancialAnalytics.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border BorderThickness="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"></RowDefinition>
                    <RowDefinition Height="2*"></RowDefinition>
                    <RowDefinition Height="2*"></RowDefinition>
                    <RowDefinition Height="2*"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Login" Height="25" Width="75"></TextBlock>
                <TextBox x:Name="UserId" Grid.Row="1"></TextBox>
                <TextBox x:Name="Password" Grid.Row="2"></TextBox>
                <DockPanel Grid.Row="3" LastChildFill="False">
                    <Button DockPanel.Dock="Right" Height="25" Width="75" x:Name="Login" Content="Login" Margin="10,0"></Button>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
