namespace FinancialAnalytics.Wrappers.Excel.Enums
{
	public enum SearchOrder
	{
		ByColumns = 2,	//Searches down through a column, then moves to the next column
		ByRows = 1	//Searches across a row, then moves to the next row.
	}
}